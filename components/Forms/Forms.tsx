import React from 'react'

const Forms = (props: any) => {
    console.log("🚀 ~ Forms ~ props:", props)
    return (
        <div>
         {props.hola}
        </div>
    )
}

export default Forms
